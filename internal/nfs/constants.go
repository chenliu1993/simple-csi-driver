package nfs

const (
	// default mounting folder
	nfsWorkingDir = "/tmp"

	seperator          = "#"
	mountPermissionKey = "mountPermission"
	serverKey          = "server"
	basedirKey         = "basedir"
	subdirKey          = "subdir"
)
